export interface UserProfile { id: string; name: string; }
export interface LandDetails { area: number; unit: string; }
export interface CapitalDetails { margin_capital: number; }
export interface BusinessCandidate { id: string; name: string; }
export interface FinancialFeasibility { project_cost: number; }
