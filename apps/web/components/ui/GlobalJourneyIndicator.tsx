import React from 'react';

export function GlobalJourneyIndicator({ currentStep }: { currentStep?: number }) {
  // Top progress bar block removed as requested, navigation is now in sidebar
  return null;
}
